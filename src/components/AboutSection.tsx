export default function AboutSection() {
  return (
    <section
      id="the-seminar"
      style={{
        background: "var(--bg-dark)",
        minHeight: "100vh",
        width: "100%",
      }}
    />
  );
}
